# ALG4H 收尾：已交付算法修复，路线独立验证留缺

2026-09-20 北京时间约 00:50。本轮在原 03:36:39 截止前提前收尾：财务/采购组合两路的六个任务包均已验收；路线预算与性能已核验，但 C1 时窗/坐标及路线排列参考解的请求超时、状态未知，不能重发或冒充完成。继续该缺口需要白天决定新的验证范围；不为用满四小时增加无验收价值的调用。

## 本轮增量与验证

- 补齐非空购物篮经真实转换器、求解器、API 和 CLI 的结果一致性，三组手算场景包括 1.49 元预算反例、显式零库存排除及多件采购。
- 新增 24 个基础场景（19 个固定种子、5 个边界场景）、51 条性质测试。穷举参考算法独立核对最优节省和预算；排列、金额缩放测试保留数量上限。有限库存下新增逐件被支配商品可以提高合法最优值，不做错误的无条件删除。
- 最后复核发现 API 把布尔值先转为金额 1/0，从而绕过求解器拒绝 bool。已加类型转换前校验，保留合法数字字符串、数字及零默认。4 条 API 失败用例先红后绿，并确认非法请求不访问数据库。

主代理最新相关 Python 集成回归 **467 通过、2 排除、4 条弃用警告**。两项 PDF 测试因隔离环境缺 Playwright，之前执行为依赖错误，本轮明确排除，不声称全仓通过。前端源文件本轮未改，最近 Node 61 通过结果沿用；路线基准独立执行记录见 [首轮报告](2026-09-20-ALG4H-R1.md)。

准确算法及性能结论保持：预算不再按整元近似；整数稀疏状态在三组合成场景与精确 Fraction 参考的商品、数量和总额一致，该三样本中位耗时约快 10.8—11.6 倍，不能推广为所有输入提速；24/42 候选仍略慢于最早不精确整元版本。不是实盘套利收益。

## 实际委派与拒收

共 15 个业务包（A6、B6、C3），21 次 Coding Plan 尝试。请求 `ark-code-latest` 的结果标识为 `auto`，另用 `doubao-seed-evolving`；最大实测并发 3。20 次有原生用量合计 167067 token，1 次 unknown 用量未知，不与主代理用量相加，不声称节省比例。额外按量服务未使用。

最终独立复核稿提出三项问题，经本地证伪：大预算触发既定状态上限约 0.024 秒，并非无界运行；销售证据已有 extra=forbid；字符串数量能正常计算。三项全部拒收。对主代理另行复现的 API 布尔转换漏洞，使用同一 A6 包的唯一质量纠正生成最小补丁，校正文件路径后通过先失败再成功的测试。早期截断或格式错误的产物仅复用经验证的完整部分；详细请求、结果和审计保留在本地 Hub。

## 保留的限制与白天决定

C1 的未知请求仍隔离，无可取回结果，未换平台重发。没有实时报价、库存、真实路网、成交结果或全东京最优性证明。原旅行文档改动保持，Notion 五张待选调研卡没有被改为已批准任务；代码只 push 隔离分支，未合并或部署。

白天待决定一项：是否按现有已验证覆盖进入合并验收，还是先另定路线独立验证的范围。当前不自动追加开发或重跑已完成任务。
